# ⚙️ Next.js Configuration (`next.config.js`)

## 📖 Overview

The `next.config.js` (or `next.config.mjs` if using ESM) file is the **central configuration** for a Next.js project.  
It allows you to customize the **build process**, **optimizations**, and **runtime behaviors** of your app.

---

## 🚀 Features & Benefits

- **Performance optimizations** → Enable compression, optimize CSS, and control bundling.
    
- **Build insights** → Bundle analyzer helps monitor bundle size.
    
- **Flexibility** → Extend Next.js defaults with experimental features and plugins.
    
- **Environment-specific config** → Different behavior in production, staging, or development.
    

---

## 🛠️ Example: `next.config.js`

```ts
// next.config.js (or next.config.mjs if using ESM)
import type { NextConfig } from "next";

const withBundleAnalyzer = require('@next/bundle-analyzer')({
  enabled: process.env.ANALYZE === 'true',
});

const nextConfig: NextConfig = {
  compress: true, // ✅ Enable gzip compression for responses
  productionBrowserSourceMaps: true, // ✅ Useful for debugging errors in production
  experimental: {
    optimizeCss: true, // ✅ Ensures Tailwind is tree-shaken and critical CSS is inlined
  },
  // Add other config options here
};

module.exports = withBundleAnalyzer(nextConfig);
```

---

## 🛠️ Customization & Configuration

- **`compress: true`**
    
    - Enables Gzip compression at the server level.
        
    - Reduces payload size for faster page loads.
        
- **`productionBrowserSourceMaps: true`**
    
    - Generates source maps in production.
        
    - Helps debug errors in error monitoring tools (e.g., Sentry, LogRocket).
        
- **`experimental.optimizeCss`**
    
    - Optimizes CSS loading.
        
    - Ensures unused Tailwind classes are tree-shaken and reduces CSS bloat.
        
- **`@next/bundle-analyzer`**
    
    - Visualizes bundle composition.
        
    - Helps identify large dependencies.
        
    - Run with:
        
        ```bash
        ANALYZE=true npm run build
        ```
        

---

## 🔒 Good Practices

- ✅ Enable **`compress`** for better performance.
    
- ✅ Use **source maps** in production only if you need debugging (can expose code if misconfigured).
    
- ✅ Use **bundle analyzer** periodically to avoid bundle size inflation.
    
- ✅ Keep **experimental flags** under review — they may change or stabilize in later Next.js versions.
    
- ✅ Organize config for **environment awareness** (production vs staging vs development).
    

> [!warning]  
> Avoid enabling unnecessary experimental features in production. Always test them in staging first.

---

## 🧪 Example: Environment-based Config

```js
const isProd = process.env.NODE_ENV === 'production';

const nextConfig = {
  compress: true,
  productionBrowserSourceMaps: !isProd ? true : false, // Debug only outside prod
  experimental: {
    optimizeCss: true,
  },
  reactStrictMode: true, // ✅ Helps catch potential issues in dev
};
```

---

## 🔗 Related

- [Next.js Configuration Docs](https://nextjs.org/docs/app/api-reference/next-config-js)
    
- [@next/bundle-analyzer](https://www.npmjs.com/package/@next/bundle-analyzer)
    
- [Next.js Performance Optimizations](https://nextjs.org/docs/app/building-your-application/optimizing)
    

---

## 🌍 Explore More

- Adding **i18n config** for multilingual sites.
    
- Configuring **image optimization** (`images.domains`).
    
- Enabling **custom webpack config** for advanced builds.
    
- Integrating **Sentry** or error tracking with source maps.
    

---

## 📚 Tags

#nextjs #next-config #performance #bundle-analyzer #good-practices
